%h3 Edit Event
#edit_error_explanation
// URL is updated by javascript, so we just use events for now
= render 'events/form', :type => :put, :pre => 'edit', :id => 'edit_event', :the_class => 'event-form-style', :url => '/events/', :button_text => 'Edit Event'